const { expect } = require("chai");
let chai = require("chai");
let chaiHttp = require("chai-http");

const should = chai.should();
const blablafunctie = require("../routes/auth");

chai.use(chaiHttp);

describe("Validation", function () {});
